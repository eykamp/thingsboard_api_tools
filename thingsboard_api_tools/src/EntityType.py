# Not used by this lib,, but still referenced by some downstream projects

class EntityType():
    CUSTOMER = "CUSTOMER"
    DASHBOARD = "DASHBOARD"
    DEVICE = "DEVICE"
    DEVICE_PROFILE = "DEVICE_PROFILE"
    TENANT = "TENANT"
